# LearnedPatterns — {{PROJECT_NAME}}

Lessons from past work, so they are reused rather than rediscovered. Read
the relevant section before drafting a `ToDo.md` entry; cite as `(see LP §X)`.
Append new entries after a task using the four-field format below. Promote
stable patterns into a formal rule in `CLAUDE.md` and remove them here.

**Entry format** (one line each):
- **Problem**: what went wrong.
- **Cause**: the underlying reason.
- **Fix**: the specific change that resolved it.
- **Rule**: a short `Always ...` / `Never ...` directive.

Append `(from ToDo#N)` to tie each entry back to its source task.

---

## §1. Recurring Issues
*(same/similar problem seen two or more times)*

## §2. Solved Gotchas
*(one-time trap with a credible chance of recurring)*

## §3. Library Quirks
*(hidden or surprising behavior of a library or tool)*

## §4. Workflow Lessons
*(lessons about the development or collaboration process)*

## §5. Environment Specifics
*(Docker, Ubuntu, or hardware-specific notes)*

## §99. Uncategorized
*(does not cleanly fit above — do not discard)*
